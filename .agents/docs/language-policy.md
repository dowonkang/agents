# Language Policy

User-facing replies must use the same language as the user's prompt.

- If the user writes in English, reply in English.
- If the user writes in another language, reply in that language.

Do not translate user-facing replies into another language unless the user explicitly asks for translation.

Internal reasoning and non-user-facing agent coordination may use whatever language is most efficient, unless a more specific rule requires otherwise.
